<h1>Edit: <%= @landmark.name %></h1>

<form action="/landmarks/<%= @landmark.id %>" method="POST">
  <input type="hidden" name="_method" value="PATCH">
  <label>Name: </label>

  <p><input type="text" name="landmark[name]" id="landmark_name" value="<%= @landmark.name %>"></p>

  <br></br>
  <h3>Select a Title:</h3>
  <%Title.all.each do |title|%>
    <label><%= title.name %></label>
    <input type="checkbox" name="landmark[title_ids][]" id="title_<%= title.id %>" value="<%=title.id%>" <%= 'checked' if @landmark.titles.include?(title) %>>
  <%end%><br>
  <h3>Select a Landmark:</h3>
  <%Landmark.all.each do |landmark|%>
  <label><%= landmark.name %></label>
    <input type="checkbox" name="landmark[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%=landmark.id%>" <%= 'checked' if @landmark.landmarks.include?(landmark) %>>
  <%end%><br><br>

  <label>And/or create new title:</label>
    <input  type="text" name="title[name]" id="new_title"></input>
  <br></br>

  <label>And/or create new landmark:</label>
    <input  type="text" name="landmark[name]" id="new_landmark"></input>
  <br></br>

  <input type="submit" value="Edit landmark">

</form>
